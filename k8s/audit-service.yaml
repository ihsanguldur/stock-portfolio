apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      containers:
        - name: audit-service
          image: audit-service:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/audit_service_db
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: portfolio
spec:
  type: NodePort
  selector:
    app: audit-service
  ports:
    - port: 8086
      targetPort: 8086
      nodePort: 30086